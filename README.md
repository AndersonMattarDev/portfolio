# Portfolio Dev

Portfolio criado com React, Vite, TypeScript e Bootstrap.

## Instalação

1. `npm install`
2. `npm run dev`

## Scripts

- `npm run dev` — inicia servidor de desenvolvimento
- `npm run build` — gera build de produção
- `npm run preview` — visualiza o build gerado
